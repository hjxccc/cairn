# 坑账

> 一行一坑。新坑追加；对策变了就地改原行（加 `修订:MM-DD`），别新旧并存。
> `spec/` 不只放坑账，还放团队规约 `spec/<主题>.md`（编码规范/领域约定/接口契约，进 git）。

- 03-18 固定周期的重试任务会同步成风暴 → 任何重放 cron 必加抖动 + 重试上限 [重试][cron]
- 03-11 价格缓存在币种切换后仍存活 → 按币种变更事件失效，单靠 TTL 不够（修订:03-13，原为"缩短 TTL"）[缓存][币种]
- 03-12 脱敏工具 <2.3 会漏邮箱域名 → 版本前置检查已进"拉脱敏生产数据到预发"SOP [pii][预发]
